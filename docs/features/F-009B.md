# F-009B: Player Combat Attack Resolution Core

## Objective

Characterize and extract bounded player attack resolution from `CombatAttack` after direction selection has already happened.

## User Value

Player attacks are the main combat action. A portable attack-resolution slice will connect existing combat predicates and future monster damage handling while keeping input, text, sound, and animation outside the core.

## Non-Goals

- Do not port direction input, combat command polling, or the full `Combat()` loop.
- Do not port projectile drawing or hit animation as platform side effects.
- Do not port spell casting or monster turns.
- Do not change weapon or Exodus Castle behavior without documentation.

## Acceptance Notes

- Caller supplies character index, direction, weapon state, random values, and combat state.
- Preserve melee, projectile, thrown dagger, Exodus Castle weapon restriction, hit chance, damage roll, and miss behavior.
- Model text, sound, projectile path, hit animation, inventory mutation, and damage handoff as explicit results or events.
- Reuse the monster damage resolution core from `F-009A` if available.

## Result

Status: complete as of 2026-06-03.

Bounded post-direction `CombatAttack` behavior is extracted as `u3_combat_attack` in `Core/src/u3_combat.c`. The caller supplies the selected direction, weapon, dagger quantity, strength, dexterity, projectile target, Exodus Castle result, and deterministic random rolls. The core resolves melee targeting through `u3_combat_monster_here`, delegates projectile path hits through the supplied projectile target, mutates thrown dagger inventory in the same order as legacy code, applies the Exodus Castle weapon restriction, evaluates hit chance, computes damage, restores hit tile state, and hands landed attacks to `u3_combat_damage_monster`.

Text, sound, redraw, pause, hit animation, miss, projectile use, and inventory changes are represented in `u3_combat_attack_result`. Projectile path calculation remains outside this slice and is represented by the caller-supplied `projectile_monster` value.

## Affected Systems

- Legacy source files: reference only, primarily `Sources/UltimaSpellCombat.c`
- Modernization harness: likely new player attack tests
- Resources/assets: none
- Build or tooling: likely `Core/include/u3_combat.h`, `Core/src/u3_combat.c`, and `harness/Makefile`
- Documentation: feature tracker, harness README, validation strategy

## Validation Plan

Run:

```bash
make -C harness test
```

Validation passed with the integrated harness.

## Open Questions

- Resolved for this extraction: projectile path calculation remains outside attack resolution; the core consumes a caller-supplied projectile hit or miss result.
- Resolved for this extraction: random behavior is represented by explicit hit and damage roll inputs so tests remain deterministic and legacy-compatible.
