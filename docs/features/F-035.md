# F-035: First Overworld Location Transition Slice

## Objective

Detect the first save-backed overworld location transition when the player explicitly uses the Enter command on a recognized town location, without implementing the full town gameplay loop.

## User Value

After movement and terrain rules work, the next major sign of a playable world is that special map locations do something recognizable. A single transition slice proves the shell can move from raw overworld walking toward real game flow while keeping destination systems bounded.

## Non-Goals

- Do not implement every transition type in one feature.
- Do not implement full town, castle, dungeon, Ambrosia, combat, or NPC gameplay.
- Do not implement legacy menu/dialog flows unless the selected transition requires a minimal placeholder.
- Do not abandon the save-domain restriction that durable writes are characterized before broad autosave behavior.

## Dependencies

- F-031 provides save-backed party position and rendering.
- F-032 provides terrain/passability and explicit movement results.
- F-034 is recommended if the transition needs user-visible status or audio feedback.
- F-036 is recommended before this feature if the selected transition mutates durable state or changes save-safety rules.
- F-013 and F-015 provide resource and map fixture context for stable transition examples.

## Acceptance Notes

- The selected first transition family is a town whose coordinates come from native `MISC` 504 location-table state and whose current Sosaria tile classifies as a town (`tile / 2 == 0x0c` in the legacy path).
- The transition is triggered by an explicit Enter command while standing on the recognized coordinate; movement onto the tile alone does not enter it.
- Recognition uses the location table and map tile rather than treating every visually similar tile as enterable.
- The result identifies the destination kind, location-table index, destination resource id, return coordinate, and initial town coordinate needed by later session loading.
- The selected transition updates explicit portable state or returns a transition request.
- The shell handles the request with a bounded status report and does not yet change `PRTY` location mode or discard the current Sosaria frame.
- Enter at an unrecognized coordinate returns an explicit not-enterable result without changing save-domain bytes.
- The current save document remains valid after the transition attempt.
- Harness coverage verifies both non-transition movement and selected transition detection.

## Affected Systems

- Legacy source files: `Sources/UltimaMain.c`, `Sources/UltimaMisc.c`, possibly `Sources/UltimaDngn.c`
- Portable core: likely `Core/include/u3_overworld.h`, `Core/src/u3_overworld.c`, and future location-state APIs if needed
- Modern shell: tick path, render refresh path, status path
- Modernization harness: `harness/overworld_movement_tests.c` or a new transition-focused harness test
- Documentation: feature tracker, selected transition contract, and manual validation notes

## Validation Plan

Add fixture-backed harness tests for selected transition detection, ordinary movement near the fixture, save-domain state validity, and shell translation of the transition request.

Run:

```bash
make -C harness test
swift test
rg -n "AppKit|SwiftUI|Metal|AVFoundation|Foundation|SDL|NS[A-Z][A-Za-z]+\\b|NSURL|Sandbox" Core
```

Manual validation:

1. Run `swift run Ultima3ModernShell`.
2. Load or create a current save with an active party.
3. Move to the documented transition fixture position.
4. Trigger the selected transition.
5. Confirm the shell reports or displays the bounded transition behavior.
6. Save and reload if the selected transition mutates durable state, and confirm the document remains valid.

## Open Questions

- Resolved for this slice: town is first because it uses the existing 2D map/render direction and does not require the dungeon perspective renderer.
- Resolved for this slice: explicit Enter matches the legacy command path; stepping onto a location is insufficient.
- Resolved for this slice: a portable transition request plus shell status is the bounded result. F-037 owns destination resource loading and F-039 owns the first actual town round trip.
- Resolved during implementation: the primary fixture is LCB Towne at location-table index 2, Sosaria coordinate `(46,19)`, tile `0x18`, and destination `MAPS` 402. Runtime detection still derives these values from save/map data instead of hard-coding the fixture.

## Status

Proposed on 2026-06-12.

Clarified on 2026-06-21 after F-036 completion: selected explicit town entry and separated detection from destination session loading.

Done on 2026-06-21. Added portable `u3_location` transition results, fixture-backed LCB Towne characterization, strict map/location-table validation, AppKit-shell tick translation for requested and unavailable Enter results, C and Swift coverage proving no save mutation, Core boundary validation, and manual test instructions. Destination loading and location-mode mutation remain deferred to F-037.
