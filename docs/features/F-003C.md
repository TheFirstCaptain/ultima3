# F-003C: Extract Pascal String Helpers

## Objective

Move the characterized Pascal string helper behavior from the harness copy into shared portable core code, then update the harness tests to call that shared implementation.

## User Value

Text and resource strings are central to the game. Extracting byte-level string helpers creates a reusable compatibility layer before modern string conversion work begins.

## Non-Goals

- Do not modernize text rendering.
- Do not convert string resources.
- Do not change the quirks captured in F-002C.

## Acceptance Notes

- Shared portable implementation covers `StringLocation`, `SearchReplace`, `IsNewline`, and `AddString`, or namespaced equivalents.
- Harness tests no longer carry local copied implementations for these functions.
- Terminal-match and length-byte quirks remain covered.
- Move/copy shims are either standard C functions in the portable module or clearly isolated test shims.

## Affected Systems

- Legacy source files: reference only
- Modernization harness: `harness/pascal_string_helpers_tests.c`, `harness/Makefile`
- Resources/assets: none
- Build or tooling: portable core source and header paths
- Documentation: validation strategy and possible architecture notes

## Validation Plan

Run:

```bash
make -C harness test
```

## Open Questions

- Should the portable API expose a legacy-compatible `Str255` typedef or a repo-specific byte string type?
